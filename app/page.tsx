import Link from "next/link";import LineChart from "@/components/LineChart";import Snapshot from "@/components/Snapshot";import {history,latest,summarize,forecast} from "@/lib/data";import {money,pct} from "@/lib/format";
export default function Home(){const h=history(),l=latest(),admins=summarize().slice(-5),fc=forecast();return <main><section className="hero"><div className="wrap heroGrid"><div><div className="eyebrow">Independent fiscal intelligence</div><h1>Understand America’s debt. <em>Past, present,</em> and possible futures.</h1><p className="lede">Explore six decades of federal debt, compare presidential administrations, and model where the nation’s fiscal trajectory may go next.</p><div className="actions"><Link className="button" href="/overview">Explore the data →</Link><Link className="button alt" href="/methodology">Read methodology</Link></div></div><div className="terminal"><div className="terminalHead"><span>U.S. TOTAL PUBLIC DEBT</span><span>● TREASURY VERIFIED</span></div><div className="mega">{money(l.total)}</div><div className="up">Updated {l.date} · prior business day</div><LineChart dark values={h.slice(-70).map(x=>x.debt)}/><div className="ticker"><span>PUBLIC {money(l.publicDebt)}</span><span>INTRAGOV {money(l.intragov)}</span></div></div></div></section><section className="section"><div className="wrap"><div className="sectionHead"><div><div className="eyebrow">Today’s fiscal position</div><h2>One number. Full context.</h2></div><p>Direct from the U.S. Treasury Fiscal Data API, with transparent definitions and source dates.</p></div><Snapshot/></div></section><section className="section white"><div className="wrap"><div className="sectionHead"><div><div className="eyebrow">Administration context</div><h2>Measure terms, not narratives.</h2></div><Link href="/administrations">Open comparison →</Link></div><div className="panel tableWrap"><table><thead><tr><th>Administration</th><th>Term-end debt</th><th>Change</th><th>Annualized</th></tr></thead><tbody>{admins.map(a=><tr key={a.president}><td><span className="dot"/>{a.president}{a.partial?" · partial":""}</td><td>{money(a.endDebt)}</td><td>{pct(a.percent)}</td><td>{pct(a.cagr)}</td></tr>)}</tbody></table></div><div className="notice">Presidents propose and administer fiscal policy, while Congress passes tax and spending legislation. Debt changes shown here occurred under both branches’ institutional control; timing does not establish causation.</div></div></section><section className="section"><div className="wrap"><div className="sectionHead"><div><div className="eyebrow">Forward intelligence</div><h2>Uncertainty belongs in the picture.</h2></div><p>Validated trend models provide ranges—not promises—and stay visually distinct from historical observations and official projections.</p></div><div className="forecastGrid"><div className="panel"><LineChart values={fc.map(x=>x.value)}/></div><div className="card"><label>10-year model range</label><strong>{money(fc[10].low)}–{money(fc[10].high)}</strong><small>Empirical 90% residual interval</small><hr/><label>20-year midpoint</label><strong>{money(fc[20].value)}</strong><small>Model-generated, not official</small></div></div></div></section><section className="section white"><div className="wrap"><div className="sectionHead"><div><div className="eyebrow">Built for scrutiny</div><h2>Research-grade foundations.</h2></div></div><div className="grid4">{[["⌁","Authoritative data","Treasury and Federal Reserve sources with visible freshness."],["◎","Administration lens","Consistent prior-observation methodology across transitions."],["△","Forecast ranges","Out-of-sample thinking with explicit model limitations."],["↗","Scenario laboratory","Stress assumptions and compare possible paths side by side."]].map(x=><div className="card feature" key={x[1]}><i>{x[0]}</i><h3>{x[1]}</h3><p>{x[2]}</p></div>)}</div></div></section></main>}

