import Snapshot from "@/components/Snapshot";import LineChart from "@/components/LineChart";import {history,latest} from "@/lib/data";import {money} from "@/lib/format";
export default function Page(){const h=history(),l=latest(),prior=h.findLast(x=>x.date<=`${Number(l.date.slice(0,4))-1}-${l.date.slice(5)}`);const yoy=prior?(l.total/prior.debt-1)*100:0;return <main className="wrap"><header className="pageHead"><div className="eyebrow">National overview</div><h1>America’s balance sheet, in focus.</h1><p>Latest observed debt and its longer historical trajectory. All amounts are nominal unless marked otherwise.</p></header><Snapshot/><div className="panel" style={{marginTop:22}}><div className="panelHeader"><h2>Debt trajectory</h2><span className="pill">Quarterly · observed</span></div><LineChart values={h.slice(-120).map(x=>x.debt)}/></div><div className="grid4"><div className="card"><label>Year-over-year</label><strong>{yoy.toFixed(1)}%</strong><small>Approx. aligned prior observation</small></div><div className="card"><label>Per resident</label><strong>{money(l.total/342_000_000)}</strong><small>Using 342.0M planning estimate</small></div><div className="card"><label>Coverage</label><strong>1966–present</strong><small>Comparable quarterly series</small></div><div className="card"><label>Lineage</label><strong>Treasury</strong><small>Distributed through Fiscal Data & FRED</small></div></div></main>}
