import LineChart from "@/components/LineChart";import {history} from "@/lib/data";import {money} from "@/lib/format";
export default function Page(){const h=history(),markers=[["2008-09","Financial crisis"],["2020-03","Pandemic"]].map(([date,label])=>({index:h.findIndex(x=>x.date>=date),label}));return <main className="wrap"><header className="pageHead"><div className="eyebrow">Historical explorer</div><h1>Six decades of fiscal history.</h1><p>Quarter-end total public debt. Daily observations are available from Treasury from April 1993 onward via the API.</p></header><div className="panel"><div className="panelHeader chartNarrative"><div><h2>How did the national debt evolve?</h2><p>Nominal debt rose gradually for decades, then accelerated around major fiscal and economic shocks.</p></div><span className="pill">Nominal USD · linear</span></div><LineChart values={h.map(x=>x.debt)} labels={h.map(x=>x.date.slice(0,4))} xLabel="Calendar year" yLabel="Total public debt · nominal USD" markers={markers}/></div><div className="panel tableWrap"><table><thead><tr><th>Date</th><th>Total public debt</th><th>Frequency</th><th>Source</th></tr></thead><tbody>{h.slice(-20).reverse().map(x=><tr key={x.date}><td>{x.date}</td><td>{money(x.debt)}</td><td>Quarterly, end period</td><td>U.S. Treasury / FRED</td></tr>)}</tbody></table></div></main>}
