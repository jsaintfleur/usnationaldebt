import type {AdminSummary} from "@/lib/types";
export default function AdministrationChart({rows}:{rows:AdminSummary[]}){const max=Math.max(...rows.map(x=>x.percent));return <div className="adminChart" role="img" aria-label="Percent growth in total public debt by presidential administration"><div className="politicalLegend"><span><i className="partyMark dem"/>Democratic president</span><span><i className="partyMark rep"/>Republican president</span></div>{rows.map(x=>{const party=x.party==="Democratic"?"dem":"rep";return <div className="adminRow" key={x.president} title={`${x.president}: debt increased ${x.percent.toFixed(1)}%, from $${(x.startDebt/1e12).toFixed(2)}T to $${(x.endDebt/1e12).toFixed(2)}T`}><div className="adminName"><i className={`partyMark ${party}`}/><span>{x.president}</span></div><div className="adminTrack"><div className={`adminBar ${party}`} style={{width:`${x.percent/max*100}%`}}/></div><div className="adminValue">+{x.percent.toFixed(1)}%</div></div>})}<div className="adminAxis"><span/><div className="adminTicks"><span>0%</span><span>{(max/2).toFixed(0)}%</span><span>{max.toFixed(0)}%</span></div><span>Change</span></div><div className="chartFoot"><span>Transition values use the nearest prior quarterly observation</span><span>Color identifies presidential party; labels carry the comparison</span></div></div>}
