"use client";
export default function LineChart({values,dark=false}:{values:number[],dark?:boolean}){const w=620,h=220,pad=18,min=Math.min(...values),max=Math.max(...values),range=Math.max(1,max-min),coords=values.map((v,i)=>({x:pad+i/(values.length-1)*(w-pad*2),y:h-pad-(v-min)/range*(h-pad*2)})),pts=coords.map(p=>`${p.x},${p.y}`).join(" "),linePath=coords.map((p,i)=>`${i?"L":"M"} ${p.x} ${p.y}`).join(" "),areaPath=`${linePath} L ${coords.at(-1)!.x} ${h-pad} L ${pad} ${h-pad} Z`,last=coords.at(-1)!;return <svg className="chart" viewBox={`0 0 ${w} ${h}`} role="img" aria-label="United States national debt trend"><defs><linearGradient id="chartFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor={dark?"#3f92ff":"#1768e5"} stopOpacity=".34"/><stop offset="1" stopColor="#1768e5" stopOpacity="0"/></linearGradient><filter id="glow"><feGaussianBlur stdDeviation="3" result="blur"/><feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge></filter></defs>{[.25,.5,.75].map(n=><line key={n} x1={pad} x2={w-pad} y1={pad+n*(h-pad*2)} y2={pad+n*(h-pad*2)} stroke={dark?"#ffffff18":"#0b234612"} strokeDasharray="3 7"/>)}<path d={areaPath} fill="url(#chartFill)"/><polyline points={pts} fill="none" stroke={dark?"#58a3ff":"#1768e5"} strokeWidth="3" strokeLinejoin="round" vectorEffect="non-scaling-stroke"/><circle cx={last.x} cy={last.y} r="5" fill="#56dfb5" stroke={dark?"#07192f":"white"} strokeWidth="3" filter="url(#glow)"/></svg>}
