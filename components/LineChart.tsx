"use client";
export default function LineChart({values,dark=false}:{values:number[],dark?:boolean}){const w=620,h=220,min=Math.min(...values),max=Math.max(...values),pts=values.map((v,i)=>`${i/(values.length-1)*w},${h-(v-min)/(max-min)*180-20}`).join(" ");return <svg className="chart" viewBox={`0 0 ${w} ${h}`} role="img" aria-label="United States national debt trend"><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#4384ef" stopOpacity=".38"/><stop offset="1" stopColor="#4384ef" stopOpacity="0"/></linearGradient></defs><path className="area" d={`M ${pts.replaceAll(","," ").replaceAll(" "," L ")} L ${w} ${h} L 0 ${h} Z`}/><polyline points={pts} fill="none" stroke={dark?"#69a1fa":"#165bd8"} strokeWidth="3" vectorEffect="non-scaling-stroke"/><circle cx={w} cy={Number(pts.split(" ").at(-1)?.split(",")[1])} r="5" fill="#64d5b4"/></svg>}
