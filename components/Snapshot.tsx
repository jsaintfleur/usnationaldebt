import { latest } from "@/lib/data";import { money } from "@/lib/format";
export default function Snapshot(){const x=latest();return <div className="grid4"><div className="card"><label>Total public debt</label><strong>{money(x.total)}</strong><small>As of {x.date}</small></div><div className="card"><label>Held by the public</label><strong>{money(x.publicDebt)}</strong><small>{(x.publicDebt/x.total*100).toFixed(1)}% of total</small></div><div className="card"><label>Intragovernmental</label><strong>{money(x.intragov)}</strong><small>Government accounts</small></div><div className="card"><label>Source freshness</label><strong>Daily</strong><small>Prior business day · Treasury</small></div></div>}
