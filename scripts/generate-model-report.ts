import fs from "node:fs";import {history} from "../lib/data";import {evaluateModels} from "../lib/forecasting";const rows=evaluateModels(history()),header="model,mae,rmse,mape,bias,selected,political_features\n",body=rows.map(x=>[x.model,x.mae,x.rmse,x.mape,x.bias,x.selected,x.politicalFeatures].join(",")).join("\n");fs.writeFileSync("MODEL_COMPARISON.csv",header+body+"\n");console.log(`Wrote ${rows.length} rolling-origin model evaluations.`);
