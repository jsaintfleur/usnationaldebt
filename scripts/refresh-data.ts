import fs from "node:fs/promises";import path from "node:path";const root=process.cwd();async function get(url:string){const r=await fetch(url,{headers:{"User-Agent":"DebtScopeAI/1.1 research"}});if(!r.ok)throw new Error(`${r.status} ${url}`);return r.text()}const sources={"debt-latest.json":"https://api.fiscaldata.treasury.gov/services/api/fiscal_service/v2/accounting/od/debt_to_penny?sort=-record_date&page%5Bsize%5D=5","historical-debt.csv":"https://fred.stlouisfed.org/graph/fredgraph.csv?id=GFDEBTN","cpi.csv":"https://fred.stlouisfed.org/graph/fredgraph.csv?id=CPIAUCSL","gdp.csv":"https://fred.stlouisfed.org/graph/fredgraph.csv?id=GDP"};const downloaded=await Promise.all(Object.entries(sources).map(async([file,url])=>[file,await get(url)] as const));for(const [file,body] of downloaded)await fs.writeFile(path.join(root,"data",file),body);console.log(`Atomically refreshed ${downloaded.length} official source snapshots.`);
