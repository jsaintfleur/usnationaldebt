# Contributing

Create a focused branch, keep source lineage visible, and never add invented or manually estimated production observations. Run `npm run data:validate`, `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build` before opening a pull request. Forecast changes require leakage-safe rolling-origin evaluation and an updated model card.
